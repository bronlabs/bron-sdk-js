export interface IntentsQuote {
  fromAmount: string;
  fromAssetId: string;
  minPrice: string;
  minToAmount: string;
  oracleFeePercent: string;
  solverFeePercent: string;
  toAmount: string;
  toAssetId: string;
}
